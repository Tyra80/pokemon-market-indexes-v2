# ============================================================
# Pokemon Market Indexes v2 - Daily Index Calculation
# ============================================================
# Calculates index values daily using Laspeyres chain-linking
# Time: 08:00 Paris (07:00 UTC) - after price fetch
# 
# Features:
# - Daily index calculation (RARE_100, RARE_500, RARE_ALL)
# - Automatic monthly rebalancing on 1st of month
# - Smart liquidity (B+C+D method with volume decay)
# ============================================================

name: Daily Index Calculation

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:
    inputs:
      force_rebalance:
        description: 'Force monthly rebalancing'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  calculate-index:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 📊 Calculate Daily Index
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          FORCE_REBALANCE: ${{ github.event.inputs.force_rebalance || 'false' }}
        run: |
          if [ "$FORCE_REBALANCE" = "true" ]; then
            echo "🔄 Force rebalancing enabled"
            python scripts/calculate_index.py --rebalance
          else
            python scripts/calculate_index.py
          fi
      
      - name: ✅ Log success
        if: success()
        run: echo "✅ Daily index calculation completed (Laspeyres chain-linking)"
      
      - name: ❌ Log failure
        if: failure()
        run: echo "❌ Daily index calculation failed"